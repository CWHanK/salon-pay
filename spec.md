# SalonFlow 系統規格說明書 (System Specification)

## 1. 專案定位與概述
**SalonFlow** 是一套專為美髮沙龍設計的雲端客單、日常流水與店務管理系統。本系統採用輕量化單頁應用程式（SPA）架構，無需繁雜的後端伺服器佈署，開啟瀏覽器即可運作，並透過 Firebase 達成即時跨裝置雲端資料同步。

---

## 2. 系統架構與技術棧
- **架構模式**：純前端單頁應用程式 (Single Page Application, SPA)
- **UI & 樣式**：Tailwind CSS (CDN)、Lucide Icons (圖示庫)
- **試算表處理**：SheetJS (`xlsx.full.min.js`) 支援客戶端 Excel 檔案匯出
- **雲端資料庫與驗證**：
  - **Firebase Authentication**：支援「自訂帳號名稱」（如 hank、amy88）與標準 Email / 密碼驗證登入與註冊機制（系統底層以 `@salon.local` 虛擬網域自動轉換，保持純前端無伺服器架構之相容性與安全性）
  - **Cloud Firestore**：雲端儲存人員、服務項目、客單紀錄與帳號資料
  - **LocalStorage**：客戶端快取與離線暫存備援

---

## 3. 角色與權限架構

系統區分為兩種角色身分：**店家管理員 (Admin)** 與 **店內人員/員工 (Staff)**。

### 3.1 店家管理員 (Admin)
- **權限**：具備系統最高完整權限。
- **功能範圍**：
  - 檢視所有開單金額與抽成明細。
  - 維護服務項目定價與預設抽成比率。
  - 管理人員名冊，並可將店內人員與「未註冊」或「已註冊」帳號進行綁定。
  - 查看全體已註冊帳號，具備**雙重確認（含 5 秒倒數安全機制）**刪除已註冊帳號之權限。
  - 查看並調整所有人員之月度抽成、發放獎金、列印正式薪資結算單。
  - 支援將自身權限切換/降級為一般人員。

### 3.2 店內人員 / 員工 (Staff)
- **權限**：僅限個人現場開單與歷史帳單流水查閱。
- **功能範圍**：
  - 現場開單（自動鎖定為自身帳號，無法切換他人）。
  - 開單時僅能看到服務定價與小計金額，**無法查看店家抽成比率與抽成金額**。
  - 查閱個人歷史客單流水、依月份與顧客姓名搜尋，並匯出個人客單明細 Excel。
  - 查看個人當月服務客數與營業額總計。
  - 無權存取「月薪結算」與「設定」之後台管理功能；可憑管理員授權密鑰即時驗證升級為管理員。

---

## 4. 核心功能模組

### 4.1 現場開單 (Live Billing)
- **開單人員綁定**：開單人員直接連動當前登入之人員，不需下拉選擇，確保責任歸屬明確。
- **一致開單介面**：無論管理者或員工，現場開單介面完全一致，專注於服務項目、定價與數量輸入，不顯示抽成比率或抽成金額，方便現場在顧客面前透明作業。
- **多項目合單**：支援同一位顧客單次結算多項技術服務或產品銷售。
- **即時金額試算**：
  - 單項金額即時以 `單價 × 數量` 結算。
  - 頂端卡片即時統計顧客現場消費總金額與服務項目數。
- **抽成背景計算**：系統依設定之後台服務抽成率自動在後端計算抽成並記入帳單記錄，供歷史查詢與月薪結算使用。
- **一鍵開單**：點擊「開單」按鈕即自動同步寫入雲端並產生唯一客單流水編號。

### 4.2 歷史帳單與流水管理 (History)
- **雙檢視模式**：
  - 電腦端：完整表格檢視。
  - 手機端：自適應卡片式檢視。
- **多維度篩選**：支援月份篩選、人員篩選（管理員可選全員，員工鎖定本人）與顧客姓名/備註關鍵字即時搜尋。
- **客單刪除**：單據刪除確認防呆機制，並即時同步刪除雲端紀錄。
- **流水匯出**：一鍵產生並下載 `.xlsx` Excel 流水帳單，並自動依使用者身分隱藏/提供抽成機密欄位。

### 4.3 月薪結算與月報表 (Monthly Payroll & Reports，管理員專屬)
- **全月業績統計**：自動累計指定月份之總客數、客單均價、總營業額與總抽成金額。
- **薪資試算結構**：
  $$\text{實發薪資} = \text{技術/產品抽成總額} + \text{其他獎金/補貼}$$
- **多工作表 Excel 匯出**：包含「月薪資結算總表」與「當月客單明細」。
- **專業薪資單列印**：內建美髮沙龍專用薪資單排版樣式，支援主管覆核簽名與員工簽收欄位，可直接列印或匯出 PDF。

### 4.4 系統設定與維護 (Settings)
1. **服務項目管理**：
   - 支援新增、編輯、刪除服務與商品，自訂名稱、定價與預設抽成比率 (%)。
2. **人員名冊管理**：
   - 支援新增/編輯店內人員姓名。
   - 支援綁定帳號，可填入「已註冊」或「未註冊」之自訂帳號（或電子郵件）。當未註冊人員後續使用該帳號名稱完成註冊時，系統將自動完成 UID 關聯對應。
3. **帳號管理與安全刪除**：
   - 管理員可查看所有已註冊之自訂帳號清單與綁定狀態。
   - 刪除帳號具備安全雙重確認機制：第一階段提示警告，第二階段跳出模態視窗並**強制等待 5 秒倒數**後方可確認刪除，避免誤刪。
4. **資料備份與還原**：
   - 支援將全部沙龍資料一鍵匯出為標準 `JSON` 備份檔，並可隨時進行還原或覆蓋。

---

## 5. 資料結構規格 (Data Schema)

```typescript
// 人員實體 (Staff)
interface Staff {
  id: string;          // 唯一識別碼 (如 stf-1)
  name: string;        // 人員姓名
  email?: string;      // 綁定之自訂帳號或信箱 (可為尚未註冊之帳號)
  linkedUid?: string;  // 關聯之 Firebase UID
}

// 服務項目實體 (Service)
interface Service {
  id: string;          // 服務識別碼 (如 srv-1)
  name: string;        // 服務或產品名稱
  price: number;       // 標準定價 ($NT)
  rate: number;        // 預設抽成比例 (%)
  category?: string;   // 服務類別 (技術服務 / 產品銷售)
}

// 開單項目明細 (OrderItem)
interface OrderItem {
  serviceId: string;   // 對應服務 ID
  name: string;        // 項目名稱
  price: number;       // 實際計價單價
  rate: number;        // 實際計價抽成率 (%)
  qty: number;         // 數量
  amount: number;      // 項目小計 (price * qty)
  commission: number;  // 項目抽成 (amount * rate / 100)
}

// 帳單實體 (Order)
interface Order {
  id: string;          // 訂單編號 (如 ord-timestamp)
  orderNo: string;     // 顯示流水單號 (如 T-20260905-XXXX)
  date: string;        // 服務日期 (YYYY-MM-DD)
  staffId: string;     // 主作人員 ID
  staffName: string;   // 主作人員姓名
  customer: string;    // 顧客稱謂 / 姓名
  notes: string;       // 備註說明
  items: OrderItem[];  // 消費項目陣列
  totalAmount: number; // 實收總額
  totalCommission: number; // 設計師抽成總額
  salonNet: number;    // 店家淨額
  createdAt: string;   // 建立時間 (ISO String)
}
```
